// question
// integers N, A, B given
// a permutation is nums [1 to N], with unique nums

// can you create two permutations p and q of size N ?
// such that their longest common prefix is of length A
// their longest common suffix is of length B
// print yes or no

// N = 4, A = 1, B = 1
// p = [1, 2, 3, 4]
// q = [1, 3, 2, 4]
// longest common prefix is [1] of length 1
// longest common suffix is [4] of length 1
// answer - yes

// Argument
// if (a+b+2 <= n || a==b==n) "YES"
// else "NO"
